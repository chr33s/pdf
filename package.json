{
  "name": "@chr33s/pdf-monorepo",
  "version": "3.0.0",
  "scripts": {
    "check": "oxfmt --check && oxlint --type-aware",
    "fix": "oxfmt && oxlint --type-aware --fix",
    "workspace": "run () { for pkg in brotli codepoints dfa restructure standard-fonts tiny-inflate unicode-trie unicode-properties upng fontkit pdf; do npm --workspace=packages/$pkg run \"$@\" --if-present || return $?; done; }; run \"$@\""
  },
  "packageManager": "npm@10.0.0+",
  "engines": {
    "node": "^22.18.0"
  },
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "oxfmt": "^0.14.0",
    "oxlint": "^1.29.0",
    "oxlint-tsgolint": "^0.8.1"
  }
}
